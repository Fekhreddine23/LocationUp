# Backend
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# Database (PostgreSQL)
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/locationup
SPRING_DATASOURCE_USERNAME=locationup
SPRING_DATASOURCE_PASSWORD=locationup

# JWT (32+ bytes secret)
APP_JWT_SECRET=change-me-to-a-strong-64-hex-or-base64-secret
APP_JWT_EXPIRATION=900000
APP_JWT_REFRESH_EXPIRATION=604800000

# CORS (comma-separated)
APP_CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1

# Stripe
STRIPE_SECRET_KEY=sk_live_replace_me
STRIPE_PUBLIC_KEY=pk_live_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
STRIPE_IDENTITY_WEBHOOK_SECRET=whsec_replace_me_identity

